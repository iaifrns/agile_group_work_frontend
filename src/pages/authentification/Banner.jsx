import "./css/Banner.css";
  {/* This is the banner component in the authentication page, it is a simple component that only render a banner image, it is used to display the banner image on the left side of the authentication page */}
const Banner = () => {
  return (
    <section className="banner">
      <img src="Mask group.png" alt="banner" />
    </section>
  
  );
};

export default Banner;